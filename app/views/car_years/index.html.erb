<h1>Listing car_years</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @car_years.each do |car_year| %>
  <tr>
    <td><%= car_year.name %></td>
    <td><%= link_to 'Show', car_year %></td>
    <td><%= link_to 'Edit', edit_car_year_path(car_year) %></td>
    <td><%= link_to 'Destroy', car_year, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car year', new_car_year_path %>
