<%= form_for(@stripe_webhook) do |f| %>
  <% if @stripe_webhook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stripe_webhook.errors.count, "error") %> prohibited this stripe_webhook from being saved:</h2>

      <ul>
      <% @stripe_webhook.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :type %><br />
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.text_field :event_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
