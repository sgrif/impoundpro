<%= simple_form_for(@car_trim) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :car_model_id %>
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
