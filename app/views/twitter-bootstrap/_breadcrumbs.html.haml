- if @breadcrumbs.present?
  %ul.breadcrumb
    - @breadcrumbs[0..-2].each do |crumb|
      %li
        = link_to crumb[:name], crumb[:url], crumb[:options]
        %span.divider= divider
    %li.active
      = content_tag :span, @breadcrumbs.last[:name], @breadcrumbs.last[:options]
